# 剪映视频助手：首版产品设计

## 1. 问题重述

原来的四个功能如果各自作为独立资料库，会迫使用户反复经历“导入、分类、筛选、勾选、确认、返回项目”。真正目标不是把资料整理得完美，而是尽快为当前视频凑齐可用的素材、文案和音乐，并安全地产出剪映草稿。

因此，首版取消四个平级的重型后台，改成一个“当前视频项目”工作台。四个功能是同一条流程上的四张任务卡，用户可以按任意顺序处理。

## 2. 信息架构

```text
项目工作台
├── 当前项目概览：比例、目标时长、完成度、最近状态
├── 1 素材：拖入 / 导入 → 自动建议 → 必要时纠错
├── 2 文案：搜索或筛选用途 → 点击采用
├── 3 音乐：按项目情绪推荐 → 试听并应用
└── 4 草稿：检查缺项与路径 → 生成安全副本
```

独立资料库仍可在后续提供，但只承担批量管理和高级维护，不阻塞主流程。

### 首页呈现规则（2026-09-14 修订）

- 首页只呈现当前项目的四步任务流，不用“找一句开头”“查看推荐”等无法预测结果的模糊按钮。
- 每张任务卡必须同时说明输入、点击后出现的界面、产出结果和当前可用状态。
- 已实现功能使用明确动作名称；未实现功能显示“后续版本”并禁用，不允许通过修改状态文字伪装成功。
- 文案入口统一称为“添加视频文案”，以后打开侧边面板，按开头、卖点、转场、结尾展示完整句子。
- 音乐入口统一称为“选择背景音乐”，打开侧边面板，展示本地音乐的导入、筛选、试听和应用操作。
- 草稿入口统一称为“预览并导出草稿”，打开生成前检查面板，展示缺项、安全边界与输出结果。
- 独立资料库导航在功能实现前明确标为“后续开放”，首页不重复摆放“项目建议”快捷按钮。

## 3. 四项功能重评估

### 素材分类

旧问题：先建分类、逐项打标签、再导入项目，整理成本发生得太早。

新主流程：

1. 拖入文件或选择文件夹。
2. 系统自动建议“商品、人物、场景、口播、其他”；用户只纠正识别错误。

默认行为：未分类素材也能立即使用；分类建议批量呈现，可一次接受；记住用户最近纠错。

首版只做：本地文件夹导入、文件基础信息、基于文件名与目录名的规则分类、逐项快速改类。暂不做云同步、自动视觉识别模型、缩略图生成和复杂标签体系。

当前实现（2026-09-13）：

- 通过系统文件夹选择器导入，递归扫描常见图片和视频格式。
- 以完整路径去重；无法访问的子目录会被跳过并计入导入摘要。
- 自动建议“商品、人物、场景、口播、其他”，下拉选择即可人工纠错。
- 列表显示媒体类型、文件大小和相对目录；源文件始终只读。
- 后续补充：拖放导入、缩略图、视频时长/分辨率读取、批量改类和分类纠错记忆。

### 内容文库

旧问题：先进入文库、逐层分类、打开详情、复制，再回到项目粘贴。

新主流程：

1. 在工作台输入关键词，或点击“开头、卖点、转场、结尾”等用途筛选。
2. 点击“采用”，片段直接加入当前项目脚本；支持立即撤销。

默认行为：优先显示当前品类和最近使用内容；卡片直接展示全文；重复片段提示但不强制拦截。

首版只做：本地片段、关键词筛选、用途筛选、加入项目。暂不做多人协作、在线知识库和 AI 改写。

当前实现（2026-09-14）：

- “添加视频文案”打开右侧覆盖面板，不离开当前项目工作台。
- 内置本地示例片段，支持按关键词以及“全部、开头、卖点、转场、结尾”即时筛选。
- 列表直接展示完整句子、用途和关键词；选择后通过唯一主按钮加入项目脚本。
- 右侧面板持续展示已加入的脚本段落，并可撤销最近一次加入。
- 重复采用不会被拦截，但会在项目状态中明确提示；新建项目会清空内存脚本。
- 当前内容只在运行期间保留；自定义文案录入和本地持久化列入后续版本。

### 背景音乐

旧问题：浏览大曲库、进入详情、试听、下载、再手工匹配视频长度。

新主流程：

1. 根据项目情绪与目标时长自动给出少量推荐。
2. 悬停或点击试听，点击“应用”；默认自动从合适段落裁切并淡出。

默认行为：推荐不超过 6 首；明确显示授权备注和时长；替换音乐不会丢失之前选择，可撤销。

首版只做：本地音乐列表、情绪筛选、试听入口和选择状态。暂不做在线曲库、付费授权判断和节拍分析。

当前实现（2026-09-14）：

- “选择背景音乐”打开右侧面板，可一次选择多首本地音频。
- 支持 MP3、WAV、M4A、AAC、WMA、FLAC 和 OGG；基于文件名与目录名建议“轻快、温暖、高级、舒缓、动感、其他”。
- 可按情绪筛选，选择后试听或应用；替换前的选择进入撤销历史。
- 音乐始终从原路径读取，不复制、不移动、不修改；选择状态当前只在本次运行期间保留。

### 剪映草稿

旧问题：多页配置轨道、路径、分辨率和导出项，且覆盖风险不明确。

新主流程：

1. 点击“检查并生成”，在一个面板内显示缺失素材、目标路径和关键摘要。
2. 无阻塞问题时生成新的草稿副本，并提供“打开所在位置”。

默认行为：永不覆盖原草稿；高级轨道配置折叠；生成前校验所有媒体路径；失败保留诊断记录和上次输入。

首版只做：生成前检查界面、适配器接口、工作目录内的预览输出。真实格式必须取得用户提供的剪映草稿样本后再实现。

当前实现（2026-09-14）：

- “预览并导出”打开右侧检查面板，逐项显示素材路径、视频文案、背景音乐和安全输出状态。
- 素材缺失或已选音乐路径失效会阻止生成；文案与音乐没有选择时允许继续并明确提示。
- 检查通过后，通过唯一适配器向 `%LocalAppData%\JianyingVideoAssistant\DraftPreviews` 创建独立目录，写出项目预览清单和说明文件。
- 输出清单包含素材路径与分类、脚本顺序和音乐选择；不读取、不修改剪映用户草稿目录。
- 当前输出不是剪映可直接打开的原生草稿。真实剪映格式仍等待样本验证，界面不会把预览冒充为原生草稿。

## 4. 跨功能体验规则

- 当前项目始终可见，切换任务不丢选择。
- 每项只有一个高亮主动作。
- 自动结果先可用、后整理；所有自动修改均可撤销。
- 空状态直接给下一步按钮，不展示无意义的空表格。
- 错误在原位置说明原因，并给“重试、定位文件、保留并跳过”之一。
- 草稿生成属于高风险边界，必须展示输出位置且只创建副本。

## 5. 首版成功标准

- 新用户无需预先配置资料库即可建立项目。
- 从导入一组素材到看到可生成草稿的状态，不超过四个主要动作。
- 任一推荐项可在一次点击内应用，并能撤销。
- 用户始终能辨认当前项目、已选择内容和草稿输出位置。
