<Window x:Class="JianyingVideoAssistant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="剪映视频助手"
        Icon="Assets/AppIcon.png"
        Width="1240" Height="860" MinWidth="1040" MinHeight="700"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}"
        SourceInitialized="OnSourceInitialized"
        Loaded="OnLoaded"
        Closed="OnClosed"
        PreviewKeyDown="OnPreviewKeyDown">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="42" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <Border Background="{DynamicResource RailBrush}" BorderBrush="{DynamicResource CardStrokeBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="14,0,10,0">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center"><Ellipse x:Name="StatusDot" Width="7" Height="7" Fill="{DynamicResource WarningBrush}" /><TextBlock x:Name="StatusText" Text="正在启动本地工作台" Margin="6,0,0,0" FontSize="12" Foreground="{DynamicResource MutedTextBrush}" /><ProgressBar x:Name="StartupProgress" Width="64" Height="3" Margin="10,0,0,0" IsIndeterminate="True" /></StackPanel>
                <Button Grid.Column="1" Content="打开数据目录" Click="OnOpenDataFolderClick" Padding="12,4" MinHeight="29" />
            </Grid>
        </Border>
        <Grid Grid.Row="1">
            <wv2:WebView2 x:Name="WorkbenchBrowser" />
            <Border x:Name="LoadingPanel" Background="{DynamicResource WindowBrush}"><StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"><ProgressBar Width="220" Height="4" IsIndeterminate="True" /><TextBlock x:Name="LoadingText" Text="正在准备本地数据库与工作台…" Margin="0,18,0,0" HorizontalAlignment="Center" FontSize="15" /></StackPanel></Border>
            <Border x:Name="ErrorPanel" Visibility="Collapsed" Background="{DynamicResource WindowBrush}"><Border Style="{StaticResource GlassCardStyle}" Width="540" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="28"><StackPanel><TextBlock Text="本地工作台无法启动" FontSize="22" FontWeight="SemiBold" /><TextBlock x:Name="ErrorDetail" Margin="0,10,0,20" Foreground="{DynamicResource MutedTextBrush}" TextWrapping="Wrap" /><StackPanel Orientation="Horizontal"><Button Content="重新启动" Click="OnRetryClick" Style="{StaticResource PrimaryButtonStyle}" MinWidth="110" /><Button Content="打开数据目录" Click="OnOpenDataFolderClick" Margin="10,0,0,0" MinWidth="130" /></StackPanel></StackPanel></Border></Border>
        </Grid>
    </Grid>
</Window>
